{
  "name": "@llm-dev-ops/test-bench-sdk",
  "version": "0.2.0",
  "description": "TypeScript SDK for LLM Test Bench - A production-grade framework for testing, validation, and benchmarking of Large Language Models",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "build:clean": "rm -rf dist && npm run build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings=0",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\" \"*.{json,md}\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\" \"*.{json,md}\"",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:coverage:check": "vitest run --coverage && node scripts/check-coverage.js",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "ci": "npm run lint && npm run typecheck && npm run test:coverage && npm run build",
    "prepublishOnly": "npm run lint && npm run typecheck && npm run build",
    "prepare": "husky install || true",
    "clean": "rm -rf dist coverage test-results .tsbuildinfo node_modules/.cache"
  },
  "keywords": [
    "llm",
    "testing",
    "benchmark",
    "validation",
    "openai",
    "anthropic",
    "claude",
    "gpt",
    "cli",
    "typescript"
  ],
  "author": "LLM Test Bench Contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/LLM-Dev-Ops/test-bench.git"
  },
  "bugs": {
    "url": "https://github.com/LLM-Dev-Ops/test-bench/issues"
  },
  "homepage": "https://github.com/LLM-Dev-Ops/test-bench#readme",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "chalk": "^5.3.0",
    "claude-flow": "^2.7.47",
    "glob": "^10.3.10",
    "openai": "^4.20.1",
    "p-limit": "^5.0.0",
    "yaml": "^2.3.4",
    "yargs": "^17.7.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/yargs": "^17.0.32",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "@vitest/coverage-v8": "^1.0.4",
    "@vitest/ui": "^1.0.4",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.8.1",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
